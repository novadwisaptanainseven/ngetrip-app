<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.novadwisapta.ngetrip">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SuksesBeliPaketActivity"></activity>
        <activity android:name=".CheckOutActivity" />
        <activity
            android:name=".AgentTravelActivity"
            android:theme="@style/Theme.MaterialComponents" />
        <activity android:name=".DetailPaketWisataActivity" />
        <activity android:name=".AddFundActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".MyProfileActivity" />
        <activity android:name=".SuccessRegisterActivity" />
        <activity android:name=".RegisterTwoActivity" />
        <activity android:name=".RegisterOneActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".GetStartedActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeActivity" />
    </application>

</manifest>